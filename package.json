{
  "name": "mirc-sim",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd client && npm install --include=dev && cd ../server && npm install --include=dev",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "build": "npm run install:all && chmod +x node_modules/.bin/* client/node_modules/.bin/* server/node_modules/.bin/* 2>/dev/null; npm run build:client && npm run build:server",
    "start": "cd server && npm start"
  }
}
